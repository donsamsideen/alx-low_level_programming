#include "main.h"
/**
* mul - this function multiply two numbers
* @a: first number
* @b: second number
* this code should run, the previous didnt
* Return: result
*/
int mul(int a, int b)
{
return (a * b);
}
